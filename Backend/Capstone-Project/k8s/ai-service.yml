apiVersion: v1
kind: Service
metadata:
  labels:
    app: ai-server
  name: ai-service
spec:
  ports:
    - name: ai-http
      port: 80
      targetPort: 3000
  selector:
    app: ai-server
  type: ClusterIP
